# Baseline mode — no Qode, pure mini-swe-agent (control group)
agent:
  agent_class: "eval.agents.qode_agent.QodeAgent"
  qode_mode: "baseline"
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: "docker"
